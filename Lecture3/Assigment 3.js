

// What is JSX ?
// JSX is not html tag but its looks like html syntax.
// JSX produces react elements like Rect.createElement().

// Superpower of JSX
// -Readability
// -Developer friendly
// -prevents XSS(cross site scriptings Attack)
// -In curly bracket you write JavaScripts

// Role of Type attribute in script tag? What options I can use there?
// The type attribute specifies the type of script.
// The type attribute identifies the content between the <script> and </script> tag. 
// Module type attribute used.

// {TitleComponent} vs {<TitleComponent/>} vs {<TitleComponent><TitleComponent/>}
// {Title Component} iss use to render react element
// {<TitleComponent/>} or {<TitleComponent><TitleComponent/>} is used to render function
//  components inside JSX or other function component.